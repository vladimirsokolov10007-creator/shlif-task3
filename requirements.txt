# ---- Ядро (обязательно) ----
# torch/torchvision: в проекте используется CPU-сборка. Для CPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Для GPU (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
torchvision>=0.17

numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
Pillow>=10.0
pyyaml>=6.0
tqdm>=4.66

# ---- Отчёты/визуализация ----
matplotlib>=3.7

# ---- UI (опционально) ----
# Streamlit ставить отдельно при необходимости демо:
#   pip install streamlit
# streamlit>=1.30
